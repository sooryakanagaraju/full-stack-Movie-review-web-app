import { ScrolrightDirective } from './scrolright.directive';

describe('ScrolrightDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrolrightDirective();
    expect(directive).toBeTruthy();
  });
});
