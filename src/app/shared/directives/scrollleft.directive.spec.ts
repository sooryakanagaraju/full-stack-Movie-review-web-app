import { ScrollleftDirective } from './scrollleft.directive';

describe('ScrollleftDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollleftDirective();
    expect(directive).toBeTruthy();
  });
});
