import { ReviewfilterPipe } from './reviewfilter.pipe';

describe('ReviewfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReviewfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
